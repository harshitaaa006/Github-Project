@page
@model FitHub.Pages.Achievements.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Achievement</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Achievement.AchievementId" />
            <div class="form-group">
                <label asp-for="Achievement.Title" class="control-label"></label>
                <input asp-for="Achievement.Title" class="form-control" />
                <span asp-validation-for="Achievement.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Achievement.Description" class="control-label"></label>
                <input asp-for="Achievement.Description" class="form-control" />
                <span asp-validation-for="Achievement.Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Achievement.DateAchieved" class="control-label"></label>
                <input asp-for="Achievement.DateAchieved" class="form-control" />
                <span asp-validation-for="Achievement.DateAchieved" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Achievement.UserId" class="control-label"></label>
                <select asp-for="Achievement.UserId" class="form-control" asp-items="ViewBag.UserId"></select>
                <span asp-validation-for="Achievement.UserId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
